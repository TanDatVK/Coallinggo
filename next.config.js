/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    output: 'export',
    basePath: '/Coalingo',
    images: {
        unoptimized: true,
    },
}

module.exports = nextConfig
